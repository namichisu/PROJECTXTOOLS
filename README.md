# PROJECTX Tools — React

## Install
npm install

## Run
npm run dev

## Build
npm run build

Built with React + JavaScript + CSS + lucide-react.
The project includes a large tool catalog and a polished React UI. Several lightweight tools are functional; advanced PDF/image tools are structured for further implementation and testing before production.
